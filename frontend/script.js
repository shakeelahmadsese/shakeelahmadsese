const API='http://localhost:4000/api';
async function loadProducts(){const q=document.getElementById('search').value;const r=await fetch(`${API}/products?q=${encodeURIComponent(q)}`);const d=await r.json();document.getElementById('products').innerHTML=d.map(p=>`<div class='card'><h3>${p.name}</h3><p>${p.description}</p><p>Age: ${p.age_group} | ${p.gender}</p><p><b>$${p.price}</b> | Stock: ${p.stock}</p><small>ID: ${p.id}</small></div>`).join('')}
async function register(){const payload={name:name.value,email:email.value,password:password.value};const r=await fetch(`${API}/auth/register`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});msg.textContent=JSON.stringify(await r.json(),null,2)}
async function login(){const payload={email:email.value,password:password.value};const r=await fetch(`${API}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const d=await r.json();if(d.token)localStorage.setItem('token',d.token);msg.textContent=JSON.stringify(d,null,2)}
async function checkout(){const token=localStorage.getItem('token');const items=itemsEl();const payload={items,payment_method:method.value,shipping_address:address.value};const r=await fetch(`${API}/orders`,{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify(payload)});msg.textContent=JSON.stringify(await r.json(),null,2)}
function itemsEl(){return document.getElementById('items').value.split(',').map(x=>{const [product_id,quantity]=x.split(':').map(Number);return {product_id,quantity}})}
loadProducts();
